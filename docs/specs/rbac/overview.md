# Trust Model

The trust model defines who is allowed to configure, operate, suspend, and observe
the D-ASA lifecycle.

The canonical role set and the RBAC enforcement rules are specified in the following
pages.
