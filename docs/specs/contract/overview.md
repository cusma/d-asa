# Contract

The contract section defines the ACTUS compliance profile, the normalized kernel
state, the schedule model, and the AVM configuration flow.

The canonical execution chain is:

```text
ACTUS contract -> AVM normalization -> AVM execution
```
